# auto-created by fixers.py


CONFIG_VALUE = None  # Auto-added shim by fixers.py (until real implementation exists).


def do_work(*_a, **_k):
    """Auto-added shim by fixers.py (until real implementation exists)."""
    pass


class HelperThing:
    """Auto-added shim by fixers.py (until real implementation exists)."""
    pass
